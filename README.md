# UPI

Internal web application used for standardized content and template generation.

## Overview
UPI is a lightweight internal tool designed to support content workflows and reduce manual setup across projects. This repository contains the current working source provided for review and internal hosting.

## Stack
- Python
- Flask

## Local Setup
```bash
pip install -r requirements.txt
python app.py
